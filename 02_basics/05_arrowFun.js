const add = (num1 , num2) => {
    return num1 + num2
}
// console.log(user(1,2));

// One line arrow function 

const sum = (n1,n2) => n1 + n2

console.log(sum(5,3));

// Arrow using objects

const user = () => ({userName : "Sam"})

console.log(user());
